[project]
name = "llmtokenburnguard-backend"
version = "0.1.0"
description = "Backend service for LLMTokenBurnGuard"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic>=2.0.0"
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
