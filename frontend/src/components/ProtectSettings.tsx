import type { Policy } from "../types/policy";

export interface ProtectSettingsProps {
  policy: Policy;
}

export function ProtectSettings(_props: ProtectSettingsProps): JSX.Element {
  // TODO: Render protect mode configuration form.
  return <section />;
}
