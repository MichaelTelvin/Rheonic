import type { Metrics } from "../types/metrics";

export interface MetricsPanelProps {
  metrics: Metrics;
}

export function MetricsPanel(_props: MetricsPanelProps): JSX.Element {
  // TODO: Render aggregate metrics widgets.
  return <section />;
}
