import type { Incident } from "../types/incident";

export interface IncidentCardProps {
  incident: Incident;
}

export function IncidentCard(_props: IncidentCardProps): JSX.Element {
  // TODO: Render incident details and evidence summary.
  return <article />;
}
