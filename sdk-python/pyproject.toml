[project]
name = "llmtokenburnguard"
version = "0.1.0"
description = "Python SDK for LLMTokenBurnGuard"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
